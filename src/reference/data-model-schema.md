# Data Model Schema

This page is generated from the current Iroha source checkout. `pnpm
get-snippets` reads `../iroha/docs/source/references/schema.json` when it is
available. If that snapshot is empty, the snippet tool runs `kagami advanced
schema` against the local `../iroha` workspace and renders the live schema.

<!--@include: @/snippets/data-model-schema.md-->
